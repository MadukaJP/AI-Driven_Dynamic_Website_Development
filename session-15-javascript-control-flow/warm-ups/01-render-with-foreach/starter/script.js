// ===================================================
// WARM-UP 1: Render a List (forEach)
// ===================================================
// The goal: take an array of strings and get every item
// showing up on the page as a list item.

// STEP 1: Here's the array and the element you'll add to.
const fruits = ["Apple", "Banana", "Cherry", "Mango"];
const list = document.querySelector('#fruit-list');

// STEP 2: Loop over `fruits` with .forEach(). For each fruit,
// add a new <li> to the list.
// Hint: list.innerHTML += `<li>${fruit}</li>`;
//
// (The += matters — it ADDS to what's already there instead of
// replacing it, so each loop turn adds one more item.)

// fruits.forEach((fruit) => {
//   ...
// });
