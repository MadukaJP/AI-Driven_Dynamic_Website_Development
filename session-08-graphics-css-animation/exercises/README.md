# Session 8 Exercises

## Exercise 1: Fix the squashed image

`squashed.html` has an image being stretched out of proportion. Fix it
using `object-fit`.

## Exercise 2: Add a hover transition

Given a plain `.button` class with no transition, add a smooth background
color change on hover (0.2s).

## Exercise 3: Reduced motion audit

Take any animation you wrote in the practical and confirm (write in
`audit.md`) that it's wrapped in a `prefers-reduced-motion` check.
