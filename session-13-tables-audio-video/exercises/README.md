# Session 13 Exercises

## Exercise 1: Merge cells

Build a table where the header "Contact Info" spans 2 columns using
`colspan`, above separate "Phone" and "Email" columns.

## Exercise 2: Add lazy loading

`no-lazy-loading.html` has 5 images with no `loading` attribute. Add
`loading="lazy"` to all but the first (the first image, likely visible
immediately, should NOT be lazy-loaded — explain why in a comment).

## Exercise 3: Accessible video

`video-no-captions.html` is missing a captions track. Add one.
