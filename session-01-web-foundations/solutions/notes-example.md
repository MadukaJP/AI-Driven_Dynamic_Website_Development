# Example answers (yours don't need to match wording)

1. HTML structures content, CSS styles it, JavaScript makes it interactive.
2. XHTML's strict rules never became necessary because browsers kept
   supporting loosely-written HTML, and HTML5 formalized a more practical
   standard.
3. Whether a given HTML/CSS/JS feature is safe to use given the browsers
   your users actually have.
